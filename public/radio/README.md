# Radio tracks

Drop your own audio files (mp3/m4a/wav/flac…) in this folder — subfolders become
stations. `npm run build` (or `npm run radio:manifest`) indexes them automatically.
The kit ships no music: the reference deployment's tracks are commercially licensed
and are not distributable. Stream stations play regardless.
