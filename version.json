{
  "version": "2026.08.16.43",
  "note": "Share cards are never shared-cached: the platform edge keyed every card URL to one cache entry regardless of its parameters, so within a cache window any card could serve another card's image. Varying the cache key per query proved unenforceable at the platform layer, so the card route now opts out of shared caching entirely - every card is rendered for the URL that asked.",
  "updateManifestUrl": "https://raw.githubusercontent.com/Irora-dev/PrismMothership/main/version.json"
}
