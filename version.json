{
  "version": "2026.08.16.42",
  "note": "Cards stop cross-serving at the edge: the card route's cache key never included its own query params on Netlify, so within a cache window every card URL could serve whichever image rendered first. The response now varies the edge cache on the full query string, so a share card is always the card that was asked for.",
  "updateManifestUrl": "https://raw.githubusercontent.com/Irora-dev/PrismMothership/main/version.json"
}
