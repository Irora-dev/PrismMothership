// OpenNext adapter config for Cloudflare Workers (docs/HOSTING.md). Only read
// by `opennextjs-cloudflare build`; inert on every other platform.
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig();
